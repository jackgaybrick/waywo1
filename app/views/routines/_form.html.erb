<%= form_for(@routine) do |f| %>
<%= f.label :routine_type %>
<%= f.collection_select :routine_type, Routine.all, :id, :routine_type %>
<% end %>

<%= button_to "Generate Routine", Routine.find(1), :method => :get, :class => "button" %>